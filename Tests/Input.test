REF 'IO' FROM 'Runtime'

{
	Tests IO console read functionality.
	Asks the user to enter the numeral 5.
	Responds appropriately.
}

? Prompts user for data
Type("Enter '5' followed by enter.")

? Reads and compares to '5'
If Read() = '5'
[
	? Print a success message
	Type("Success!")
]
? Else
[
	? Print a failure message
	Type("FAILED")
]